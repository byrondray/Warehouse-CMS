<h2>Orders</h2>

<p>
    <a href="#Orders/Create" class="btn btn-primary">Create New Order</a>
</p>

<table class="table">
    <thead>
        <tr>
            <th>Order ID</th>
            <th>Date</th>
            <th>Customer</th>
            <th>Status</th>
            <th>Total Amount</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Id</td>
                <td>@item.OrderDate.ToString("dd/MM/yyyy")</td>
                <td>@(item.Customer?.Name ?? "N/A")</td>
                <td>@(item.OrderStatus?.Status ?? "N/A")</td>
                <td>@item.TotalAmount.ToString("C")</td>
                <td>
                    <a href="#Orders/Details/@item.Id" class="btn btn-info btn-sm">Details</a>
                </td>
            </tr>
        }
    </tbody>
</table>